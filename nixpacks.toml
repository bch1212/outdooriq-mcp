[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
# NOTE: Railway exec's startCommand without a shell, so $PORT does not expand.
# Use a Python entry that reads PORT from os.environ.
cmd = "python -m run"
